# Server
PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET=your-super-secret-key-min-32-chars
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Token rotation blacklist (optional, for strict logout invalidation)
# Redis recommended in production; comma-separated set for dev
TOKEN_BLACKLIST=""
